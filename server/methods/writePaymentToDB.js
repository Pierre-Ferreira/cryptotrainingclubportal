import { Meteor } from 'meteor/meteor';
const atob = require('atob');
const parse = require('csv-parse');
import { Accounts } from 'meteor/accounts-base';
import moment from 'moment/moment';

Meteor.methods({
  writePaymentToDB(clientPaymentData) {
    
  }
})
